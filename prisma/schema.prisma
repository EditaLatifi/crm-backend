model VacationRequest {
  id          String              @id @default(uuid())
  userId      String
  user        User                @relation("VacationRequests", fields: [userId], references: [id])
  startDate   DateTime
  endDate     DateTime
  days        Int
  type        VacationType        @default(VACATION)
  status      VacationStatus      @default(PENDING)
  note        String?
  adminNote   String?
  reviewedByUserId String?
  reviewedBy  User?               @relation("VacationReviewer", fields: [reviewedByUserId], references: [id])
  reviewedAt  DateTime?
  createdAt   DateTime            @default(now())
  updatedAt   DateTime            @updatedAt
}

enum VacationType {
  VACATION
  SICK
  UNPAID
  OTHER
}

enum VacationStatus {
  PENDING
  APPROVED
  REJECTED
}

model Notification {
  id         String           @id @default(uuid())
  userId     String
  user       User             @relation("UserNotifications", fields: [userId], references: [id], onDelete: Cascade)
  type       NotificationType
  title      String
  body       String
  entityType String?
  entityId   String?
  href       String?
  read       Boolean          @default(false)
  createdAt  DateTime         @default(now())

  @@index([userId, read])
  @@index([userId, createdAt])
}

enum NotificationType {
  TASK_ASSIGNED
  TASK_COMMENT
  DEAL_STAGE_CHANGED
  PERMIT_STATUS_CHANGED
  VACATION_REVIEWED
}

model EmailLog {
  id          String   @id @default(uuid())
  subject     String
  body        String?
  direction   EmailDirection @default(OUTBOUND)
  accountId   String?
  account     Account?  @relation(fields: [accountId], references: [id])
  contactId   String?
  contact     Contact?  @relation(fields: [contactId], references: [id])
  loggedByUserId String
  loggedBy    User      @relation("EmailLogCreator", fields: [loggedByUserId], references: [id])
  loggedAt    DateTime  @default(now())
}

enum EmailDirection {
  INBOUND
  OUTBOUND
}

model Appointment {
  id          String   @id @default(uuid())
  title       String
  description String?
  startAt     DateTime
  endAt       DateTime
  accountId   String?
  account     Account?  @relation(fields: [accountId], references: [id])
  contactId   String?
  contact     Contact?  @relation(fields: [contactId], references: [id])
  dealId      String?
  deal        Deal?     @relation(fields: [dealId], references: [id])
  createdByUserId String
  creator     User      @relation("AppointmentCreator", fields: [createdByUserId], references: [id])
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@index([startAt])
  @@index([createdByUserId])
}

model Attachment {
  id        String   @id @default(uuid())
  dealId    String
  deal      Deal     @relation(fields: [dealId], references: [id])
  url       String
  filename  String
  uploadedByUserId String
  uploadedBy User   @relation("uploadedBy", fields: [uploadedByUserId], references: [id])
  createdAt DateTime @default(now())
}

model Note {
  id        String   @id @default(uuid())
  dealId    String
  deal      Deal     @relation(fields: [dealId], references: [id])
  content   String
  createdByUserId String
  createdBy User   @relation("createdBy", fields: [createdByUserId], references: [id])
  createdAt DateTime @default(now())
}
model Comment {
  id        String   @id @default(uuid())
  taskId    String
  task      Task     @relation("TaskComments", fields: [taskId], references: [id], onDelete: Cascade)
  authorId  String
  author    User     @relation("UserComments", fields: [authorId], references: [id])
  text      String
  createdAt DateTime @default(now())
}

model TaskHistory {
  id        String   @id @default(uuid())
  taskId    String
  task      Task     @relation("TaskHistory", fields: [taskId], references: [id], onDelete: Cascade)
  action    String
  payload   Json?
  createdAt DateTime @default(now())
  userId    String
  user      User     @relation("UserTaskHistory", fields: [userId], references: [id])
}
// This is the main Prisma schema for the CRM system
// It covers all required entities and relationships

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id           String   @id @default(uuid())
  email        String   @unique
  passwordHash String
  name         String
  role         Role
  createdAt    DateTime @default(now())
  accounts     Account[] @relation("AccountOwner")
  createdAccounts Account[] @relation("AccountCreator")
  deals        Deal[]    @relation("DealOwner")
  createdDeals Deal[]    @relation("DealCreator")
  assignedTasks Task[]   @relation("TaskAssignee")
  createdTasks Task[]    @relation("TaskCreator")
  timeEntries  TimeEntry[]
  runningTimer RunningTimer?
  activities   Activity[] @relation("ActivityActor")
  dailyTimeByUser DailyTimeByUser[]
  dealStageHistories DealStageHistory[]
  comments     Comment[] @relation("UserComments")
  taskHistory  TaskHistory[] @relation("UserTaskHistory")
  uploadedAttachments Attachment[] @relation("uploadedBy")
  createdNotes Note[] @relation("createdBy")
  appointments     Appointment[]    @relation("AppointmentCreator")
  emailLogs        EmailLog[]       @relation("EmailLogCreator")
  vacationRequests VacationRequest[] @relation("VacationRequests")
  reviewedVacations VacationRequest[] @relation("VacationReviewer")
  ownedProjects      Project[]         @relation("ProjectOwner")
  createdProjects    Project[]         @relation("ProjectCreator")
  projectMemberships ProjectMember[]   @relation("ProjectMembers")
  completedPhases    ProjectPhase[]    @relation("PhaseCompletedBy")
  createdPermits     Permit[]          @relation("PermitCreator")
  permitHistories    PermitHistory[]   @relation("PermitHistoryActor")
  createdVendors     Vendor[]          @relation("VendorCreator")
  budgetItems        BudgetItem[]      @relation("BudgetItemCreator")
  projectDocuments   ProjectDocument[] @relation("DocumentUploader")
  projectShares      ProjectShare[]    @relation("ShareCreator")
  notifications      Notification[]    @relation("UserNotifications")
}

enum Role {
  ADMIN
  USER
}

model Account {
  id             String    @id @default(uuid())
  name           String
  type           AccountType
  address        String?
  phone          String?
  email          String?
  notes          String?
  ownerUserId    String?
  owner          User?     @relation("AccountOwner", fields: [ownerUserId], references: [id])
  createdByUserId String
  creator        User      @relation("AccountCreator", fields: [createdByUserId], references: [id])
  contacts       Contact[]
  deals          Deal[]
  tasks          Task[]
  timeEntries    TimeEntry[]
  runningTimers  RunningTimer[]
  appointments   Appointment[]
  emailLogs      EmailLog[]
  projects       Project[]
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt
  dailyTimeByAccount DailyTimeByAccount[]

  @@index([ownerUserId])
  @@index([createdByUserId])
  @@index([type])
}

enum AccountType {
  CLIENT
  POTENTIAL_CLIENT
  PARTNER
}

model Contact {
  id           String    @id @default(uuid())
  accountId    String?
  account      Account?  @relation(fields: [accountId], references: [id])
  name         String
  email        String
  phone        String?
  title        String?
  notes        String?
  followUpDate DateTime?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  tasks        Task[]
  appointments Appointment[]
  emailLogs    EmailLog[]

  @@index([accountId])
  @@index([followUpDate])
}

model DealStage {
  id      String  @id @default(uuid())
  name    String
  order   Int
  isWon   Boolean @default(false)
  isLost  Boolean @default(false)
    deals   Deal[]
    histories DealStageHistory[] @relation("ToStage")
    fromHistories DealStageHistory[] @relation("FromStage")
}

model Deal {
  id             String   @id @default(uuid())
  accountId      String
  account        Account  @relation(fields: [accountId], references: [id])
  name           String
  stageId        String
  stage          DealStage @relation(fields: [stageId], references: [id])
  amount         Float
  currency       String
  probability    Int      // Probability to win (0-100)
  dealScore      Int      @default(0) // Calculated score for the deal
  expectedCloseDate DateTime
  followUpDate   DateTime?
  customFields   Json? // Flexible custom fields per deal
  ownerUserId    String?
  owner          User?    @relation("DealOwner", fields: [ownerUserId], references: [id])
  createdByUserId String
  creator        User     @relation("DealCreator", fields: [createdByUserId], references: [id])
  tasks          Task[]
  attachments    Attachment[]
  notes          Note[]
  stageHistories DealStageHistory[]
  appointments   Appointment[]
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt

  @@index([stageId])
  @@index([accountId])
  @@index([ownerUserId])
  @@index([createdByUserId])
  @@index([followUpDate])
  @@index([expectedCloseDate])
}

model DealStageHistory {
  id            String   @id @default(uuid())
  dealId        String
  deal          Deal     @relation(fields: [dealId], references: [id])
  fromStageId   String?
  toStageId     String
  toStage       DealStage  @relation("ToStage", fields: [toStageId], references: [id])
  fromStage     DealStage? @relation("FromStage", fields: [fromStageId], references: [id])
  changedByUserId String
  changedBy     User      @relation(fields: [changedByUserId], references: [id])
  changedAt     DateTime  @default(now())
}

model Task {
  id              String   @id @default(uuid())
  title           String
  description     String?
  status          TaskStatus
  priority        TaskPriority
  dueDate         DateTime?
  assignedToUserId String?
  assignee        User?    @relation("TaskAssignee", fields: [assignedToUserId], references: [id])
  createdByUserId String
  creator         User     @relation("TaskCreator", fields: [createdByUserId], references: [id])
  accountId       String?
  account         Account? @relation(fields: [accountId], references: [id])
  contactId       String?
  contact         Contact? @relation(fields: [contactId], references: [id])
  // already has activities and dailyTimeByTask
  dealId          String?
  deal            Deal?    @relation(fields: [dealId], references: [id])
  timeEntries     TimeEntry[]
  runningTimers   RunningTimer[]
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  dailyTimeByTask DailyTimeByTask[]
  comments        Comment[] @relation("TaskComments")
  history         TaskHistory[] @relation("TaskHistory")
  estimate        Int?

  @@index([status])
  @@index([dueDate])
  @@index([assignedToUserId])
  @@index([createdByUserId])
  @@index([accountId])
}

enum TaskStatus {
  OPEN
  IN_PROGRESS
  DONE
}

enum TaskPriority {
  LOW
  MEDIUM
  HIGH
}

model TimeEntry {
  id          String   @id @default(uuid())
  userId      String
  user        User     @relation(fields: [userId], references: [id])
  accountId   String
  account     Account  @relation(fields: [accountId], references: [id])
  taskId      String?
  task        Task?    @relation(fields: [taskId], references: [id])
  startedAt   DateTime
  endedAt     DateTime
  durationMinutes Int
  description String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  @@index([userId, startedAt])
  @@index([accountId, startedAt])
}

model RunningTimer {
  id          String   @id @default(uuid())
  userId      String   @unique
  user        User     @relation(fields: [userId], references: [id])
  accountId   String
  account     Account  @relation(fields: [accountId], references: [id])
  taskId      String?
  task        Task?    @relation(fields: [taskId], references: [id])
  startedAt   DateTime
  description String?
}

model Activity {
  id          String   @id @default(uuid())
  actorUserId String
  actor       User     @relation("ActivityActor", fields: [actorUserId], references: [id])
  entityType  String
  entityId    String
  action      String
  payloadJson Json
  createdAt   DateTime @default(now())
  // Relations for filtering
    // No foreign keys for entityId (polymorphic reference)
  @@index([entityType, entityId])
  @@index([createdAt])
}

// Optional: Daily Aggregates
model DailyTimeByUser {
  date      DateTime
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  totalMinutes Int
  @@id([date, userId])
}

model DailyTimeByAccount {
  date      DateTime
  accountId String
  account   Account  @relation(fields: [accountId], references: [id])
  totalMinutes Int
  @@id([date, accountId])
}

model DailyTimeByTask {
  date      DateTime
  taskId    String
  task      Task     @relation(fields: [taskId], references: [id])
  totalMinutes Int
  @@id([date, taskId])
}

// ─── Project Phase Tracker ───────────────────────────────────────────────────

model Project {
  id              String        @id @default(uuid())
  name            String
  description     String?
  status          ProjectStatus @default(ACTIVE)
  type            ProjectType   @default(ARCHITECTURE)
  address         String?
  budget          Float?
  currency        String        @default("CHF")
  startDate       DateTime?
  expectedEndDate DateTime?
  actualEndDate   DateTime?
  notes           String?
  accountId       String?
  account         Account?      @relation(fields: [accountId], references: [id])
  ownerUserId     String
  owner           User          @relation("ProjectOwner", fields: [ownerUserId], references: [id])
  createdByUserId String
  creator         User          @relation("ProjectCreator", fields: [createdByUserId], references: [id])
  phases          ProjectPhase[]
  members         ProjectMember[]
  permits         Permit[]
  budgetItems     BudgetItem[]
  documents       ProjectDocument[]
  vendors         ProjectVendor[]
  shares          ProjectShare[]
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt

  @@index([ownerUserId])
  @@index([accountId])
  @@index([status])
}

model ProjectPhase {
  id                String      @id @default(uuid())
  projectId         String
  project           Project     @relation(fields: [projectId], references: [id], onDelete: Cascade)
  name              String
  description       String?
  order             Int
  status            PhaseStatus @default(PENDING)
  startDate         DateTime?
  endDate           DateTime?
  notes             String?
  completedAt       DateTime?
  completedByUserId String?
  completedBy       User?       @relation("PhaseCompletedBy", fields: [completedByUserId], references: [id])
  createdAt         DateTime    @default(now())
  updatedAt         DateTime    @updatedAt

  @@index([projectId])
  @@index([status])
}

model ProjectMember {
  id        String   @id @default(uuid())
  projectId String
  project   Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  userId    String
  user      User     @relation("ProjectMembers", fields: [userId], references: [id])
  role      String?
  addedAt   DateTime @default(now())

  @@unique([projectId, userId])
  @@index([userId])
}

enum ProjectStatus {
  ACTIVE
  ON_HOLD
  COMPLETED
  CANCELLED
}

enum ProjectType {
  ARCHITECTURE
  INTERIOR_DESIGN
  CONSTRUCTION_MANAGEMENT
  VISUALIZATION
  REAL_ESTATE
  DIGITIZATION
}

enum PhaseStatus {
  PENDING
  IN_PROGRESS
  COMPLETED
  SKIPPED
}

// ─── Building Permit Pipeline ─────────────────────────────────────────────────

model Permit {
  id                  String         @id @default(uuid())
  projectId           String
  project             Project        @relation(fields: [projectId], references: [id], onDelete: Cascade)
  title               String
  authority           String?
  referenceNumber     String?
  status              PermitStatus   @default(VORBEREITUNG)
  submittedAt         DateTime?
  expectedDecisionAt  DateTime?
  decidedAt           DateTime?
  notes               String?
  documents           Json?
  createdByUserId     String
  creator             User           @relation("PermitCreator", fields: [createdByUserId], references: [id])
  history             PermitHistory[]
  createdAt           DateTime       @default(now())
  updatedAt           DateTime       @updatedAt

  @@index([projectId])
  @@index([status])
}

model PermitHistory {
  id          String       @id @default(uuid())
  permitId    String
  permit      Permit       @relation(fields: [permitId], references: [id], onDelete: Cascade)
  fromStatus  PermitStatus?
  toStatus    PermitStatus
  notes       String?
  actorUserId String
  actor       User         @relation("PermitHistoryActor", fields: [actorUserId], references: [id])
  createdAt   DateTime     @default(now())

  @@index([permitId])
}

enum PermitStatus {
  VORBEREITUNG
  EINGEREICHT
  IN_PRUEFUNG
  NACHFORDERUNG
  BEWILLIGT
  ABGELEHNT
  ZURUECKGEZOGEN
}

// ─── Vendor / Subcontractor Directory ────────────────────────────────────────

model Vendor {
  id              String          @id @default(uuid())
  name            String
  type            VendorType      @default(SONSTIGES)
  contactName     String?
  phone           String?
  email           String?
  address         String?
  notes           String?
  rating          Int?
  createdByUserId String
  creator         User            @relation("VendorCreator", fields: [createdByUserId], references: [id])
  projects        ProjectVendor[]
  createdAt       DateTime        @default(now())
  updatedAt       DateTime        @updatedAt

  @@index([type])
}

model ProjectVendor {
  id          String   @id @default(uuid())
  projectId   String
  project     Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  vendorId    String
  vendor      Vendor   @relation(fields: [vendorId], references: [id], onDelete: Cascade)
  phase       String?
  notes       String?
  assignedAt  DateTime @default(now())

  @@unique([projectId, vendorId])
  @@index([vendorId])
}

enum VendorType {
  ARCHITEKT
  BAUINGENIEUR
  ELEKTRIKER
  SANITAER
  HEIZUNG_LUEFTUNG
  MALER
  ZIMMERMANN
  DACHDECKER
  GARTENBAU
  GENERALUNTERNEHMER
  SONSTIGES
}

// ─── Budget & Cost Tracking ───────────────────────────────────────────────────

model BudgetItem {
  id              String         @id @default(uuid())
  projectId       String
  project         Project        @relation(fields: [projectId], references: [id], onDelete: Cascade)
  category        BudgetCategory @default(SONSTIGES)
  description     String
  estimatedCost   Float          @default(0)
  actualCost      Float?
  phase           String?
  notes           String?
  createdByUserId String
  creator         User           @relation("BudgetItemCreator", fields: [createdByUserId], references: [id])
  createdAt       DateTime       @default(now())
  updatedAt       DateTime       @updatedAt

  @@index([projectId])
  @@index([category])
}

enum BudgetCategory {
  HONORARE
  ROHBAU
  ELEKTRO
  SANITAER
  HEIZUNG
  FASSADE
  DACH
  INNENAUSBAU
  UMGEBUNG
  RESERVE
  SONSTIGES
}

// ─── Document Vault ───────────────────────────────────────────────────────────

model ProjectDocument {
  id              String           @id @default(uuid())
  projectId       String
  project         Project          @relation(fields: [projectId], references: [id], onDelete: Cascade)
  name            String
  url             String
  category        DocumentCategory @default(SONSTIGES)
  version         String?
  notes           String?
  uploadedByUserId String
  uploadedBy      User             @relation("DocumentUploader", fields: [uploadedByUserId], references: [id])
  createdAt       DateTime         @default(now())

  @@index([projectId])
  @@index([category])
}

enum DocumentCategory {
  GRUNDRISS
  SCHNITT
  ANSICHT
  BAUGESUCH
  VERTRAG
  OFFERTE
  RECHNUNG
  PROTOKOLL
  FOTO
  SONSTIGES
}

// ─── Client Share Link ────────────────────────────────────────────────────────

model ProjectShare {
  id              String    @id @default(uuid())
  projectId       String
  project         Project   @relation(fields: [projectId], references: [id], onDelete: Cascade)
  token           String    @unique @default(uuid())
  label           String?
  expiresAt       DateTime?
  createdByUserId String
  creator         User      @relation("ShareCreator", fields: [createdByUserId], references: [id])
  createdAt       DateTime  @default(now())

  @@index([projectId])
  @@index([token])
}
